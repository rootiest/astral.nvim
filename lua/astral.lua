local astral = require("astral.init")

return {
  setup = astral.setup,
  restore_colorscheme = astral.restore_colorscheme,
  reset_colorscheme = astral.reset_colorscheme,
  config = astral.config,
}
